{% extends 'base.tmpl' %}
{% block title %}{{ title }}{% endblock %}
{% block content %}
  <div class="container">
    <div id="editform">
      <span>title:
        <input type="text" class="input-block-level" id="title" name="title" value="{{ place.title }}">
      </span>
      <span>author:
        <input type="text" class="input-block-level" id="author" name="author" value="{{ place.author }}">
      </span>
      <span>location:
        <input type="text" class="input-block-level" id="place_name" name="place_name" value="{{ place.scenelocation}}">
      </span>
      <span>scene:
        <input type="text" class="input-block-level" id="scene" name="scene" value="{{ place.scene }}">
      </span>
      <span>description:
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.scenedescription }}">
      </span>
      <span>notes:
        <input type="text" class="input-block-level" id="notes" name="notes" value="{{ place.notes }}">
      </span>
      <span>image url:
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.image_url }}">
      </span>
      <span>actors:
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.actors }}">
      </span>
      <span>time of day:
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.scenetime }}">
      </span>
      <span>symbols:
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.symbols }}">
      </span>
      <span>user-submitted isbn
        <input type="text" class="input-block-level" id="image_url" name="image_url" value="{{ place.ug_isbn }}">
      </span>
      <span id="addplacebutton" class="btn placesubmit">Save Place</span>
      <span id="addplacebutton" class="btn placesubmit">Delete Place</span>
    </div>
  </div>
{% endblock %}
{% block scripts %}
  <script src="/js/src/app.js"></script>
{% endblock %}